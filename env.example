# Create a new file called '.env' and copy the contents of this file into it.   
# Then, replace the placeholder values with your actual configuration.

# Confluence Configuration
# -----------------------
# Base URL of your Confluence instance (including protocol)
CONFLUENCE_URL=https://your-confluence-instance

# Confluence authentication - either use username/password or API token
CONFLUENCE_USERNAME=your-username
CONFLUENCE_PASSWORD=your-password
CONFLUENCE_API_TOKEN=your-confluence-api-token

# Optional: Specific Confluence space key to migrate from
# Leave empty to migrate from all spaces
CONFLUENCE_SPACE_KEY=

# Discourse Configuration
# ----------------------
# Base URL of your Discourse instance (including protocol)
DISCOURSE_URL=https://your-discourse-instance

# API key from Discourse (generate in admin settings)
DISCOURSE_API_KEY=your-discourse-api-key

# Username of the Discourse account that will perform the migration
# Recommended to use 'system' for administrative tasks
DISCOURSE_API_USERNAME=system
